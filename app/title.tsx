import { Metadata } from 'next'

export const metadata: Metadata = {
    title: {
      template: '%s | Documentation',
      default: 'Documentation', // a default is required when creating a template
    },
  }